import { createClient } from "npm:@supabase/supabase-js@2";
import { buildHourlyReport, derivePolicySnapshot, formatTelegramReport, type HourlyReportPayload, type ShadowTick } from "./logic.ts";

const SUPABASE_URL=Deno.env.get("SUPABASE_URL")!;const SERVICE_ROLE_KEY=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;const supabase=createClient(SUPABASE_URL,SERVICE_ROLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}});
const SOURCE_EXPERIMENT_ID="phase37-prospective-live-20260903";const EVIDENCE_CLASS="PROSPECTIVE_DEVELOPMENT_SHADOW";const AUTH_ID="control-v2";
const ALLOWED_ORIGIN=/^https:\/\/monster-coins(?:-pro)?-[a-z0-9-]*oemer-yildirim\.vercel\.app$/i;const ALLOWED_EXACT=new Set(["https://monster-coins-pro-seven.vercel.app","https://monster-coins-pro-oemer-yildirim.vercel.app","https://monster-coins-pro-git-brian-2026-oemer-yildirim.vercel.app","http://localhost:3000","http://127.0.0.1:3000"]);

type SessionEvent={event_id:string;session_id:string;event_kind:"START"|"PAUSE";requested_at:string;starting_equity:number|string|null;policy_scope:"BOTH"|"NATIVE"|"PROFIT"|null;source_experiment_id:string};type SessionState={start:SessionEvent;last_event:SessionEvent;active:boolean;ended_at:string|null};type ReportRow={report_id:string;session_id:string;policy_kind:"NATIVE"|"PROFIT";window_start:string;window_end:string;generated_at:string;payload:HourlyReportPayload;telegram_attempted:boolean;telegram_sent:boolean;telegram_error:string|null};
function corsHeaders(origin?:string|null):Record<string,string>{const allowed=origin&&(ALLOWED_EXACT.has(origin)||ALLOWED_ORIGIN.test(origin))?origin:"https://monster-coins-pro-oemer-yildirim.vercel.app";return{"access-control-allow-origin":allowed,"access-control-allow-headers":"content-type,x-brian-dashboard-key,x-brian-cron-key","access-control-allow-methods":"POST,OPTIONS","vary":"Origin"}}
function jsonResponse(body:unknown,status=200,origin?:string|null):Response{return new Response(JSON.stringify(body),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store",...corsHeaders(origin)}})}
async function sha256Hex(value:string):Promise<string>{const bytes=new TextEncoder().encode(value),digest=new Uint8Array(await crypto.subtle.digest("SHA-256",bytes));return[...digest].map(b=>b.toString(16).padStart(2,"0")).join("")}
function constantTimeEqual(left:string,right:string):boolean{if(left.length!==right.length)return false;let diff=0;for(let i=0;i<left.length;i++)diff|=left.charCodeAt(i)^right.charCodeAt(i);return diff===0}
async function authHashes():Promise<{dashboard:string;cron:string}>{const result=await supabase.from("brian_dashboard_auth").select("dashboard_key_sha256,cron_key_sha256").eq("auth_id",AUTH_ID).single();if(result.error||!result.data)throw new Error(`dashboard auth unavailable: ${result.error?.message??"missing row"}`);return{dashboard:String(result.data.dashboard_key_sha256),cron:String(result.data.cron_key_sha256)}}
async function requireDashboardAuth(req:Request){const supplied=(req.headers.get("x-brian-dashboard-key")??"").trim();if(!supplied)throw new Error("UNAUTHORIZED_DASHBOARD");const expected=(await authHashes()).dashboard;if(!constantTimeEqual(await sha256Hex(supplied),expected))throw new Error("UNAUTHORIZED_DASHBOARD")}
async function requireCronAuth(req:Request){const supplied=(req.headers.get("x-brian-cron-key")??"").trim();if(!supplied)throw new Error("UNAUTHORIZED_CRON");const expected=(await authHashes()).cron;if(!constantTimeEqual(await sha256Hex(supplied),expected))throw new Error("UNAUTHORIZED_CRON")}
async function latestSession():Promise<SessionState|null>{const latest=await supabase.from("brian_dashboard_session_events").select("event_id,session_id,event_kind,requested_at,starting_equity,policy_scope,source_experiment_id").order("requested_at",{ascending:false}).order("event_id",{ascending:false}).limit(1).maybeSingle();if(latest.error)throw latest.error;if(!latest.data)return null;const last=latest.data as SessionEvent;const startResult=await supabase.from("brian_dashboard_session_events").select("event_id,session_id,event_kind,requested_at,starting_equity,policy_scope,source_experiment_id").eq("session_id",last.session_id).eq("event_kind","START").order("requested_at",{ascending:true}).limit(1).single();if(startResult.error||!startResult.data)throw new Error(`session start missing for ${last.session_id}`);return{start:startResult.data as SessionEvent,last_event:last,active:last.event_kind==="START",ended_at:last.event_kind==="PAUSE"?last.requested_at:null}}
async function loadTicks(experimentId:string,policy:"NATIVE"|"PROFIT",startAt:string,endAt:string|null):Promise<ShadowTick[]>{const pageSize=1000,out:ShadowTick[]=[];for(let from=0;;from+=pageSize){let query=supabase.from("brian_live_shadow_ticks").select("observed_at,equity_after_costs,period_pnl,trading_cost,max_drawdown_pct_after,target_weights,drifted_weights,observed_mid_prices,observed_spread_bps,diagnostics").eq("experiment_id",experimentId).eq("policy_kind",policy).gte("observed_at",startAt).order("observed_at",{ascending:true}).range(from,from+pageSize-1);if(endAt)query=query.lte("observed_at",endAt);const result=await query;if(result.error)throw result.error;const page=(result.data??[]) as ShadowTick[];out.push(...page);if(page.length<pageSize)break}return out}
async function latestEngineTick(policy:"NATIVE"|"PROFIT"){const result=await supabase.from("brian_live_shadow_ticks").select("observed_at,equity_after_costs,target_weights,diagnostics").eq("experiment_id",SOURCE_EXPERIMENT_ID).eq("policy_kind",policy).order("observed_at",{ascending:false}).limit(1).maybeSingle();if(result.error)throw result.error;return result.data}
function topDecisionDiagnostics(ticks:ShadowTick[]){return ticks.slice(-12).reverse().map(tick=>{const diagnostics=(tick.diagnostics??{}) as Record<string,unknown>,model=(diagnostics.model??{}) as Record<string,Record<string,unknown>>,ranked=Object.entries(model).map(([symbol,data])=>({symbol,raw_edge:Number(data.raw_edge??0),selection_score:data.selection_score==null?null:Number(data.selection_score),prediction:Number(data.prediction??0),uncertainty:Number(data.uncertainty??0),selected_weight:Number(data.selected_weight??0)})).sort((a,b)=>Math.abs(b.raw_edge)-Math.abs(a.raw_edge));return{observed_at:tick.observed_at,active:Boolean(diagnostics.active),target_weights:tick.target_weights,top_candidates:ranked.slice(0,3)}})}
async function loadReports(sessionId:string):Promise<ReportRow[]>{const result=await supabase.from("brian_dashboard_hourly_reports").select("report_id,session_id,policy_kind,window_start,window_end,generated_at,payload,telegram_attempted,telegram_sent,telegram_error").eq("session_id",sessionId).order("window_end",{ascending:false}).limit(24);if(result.error)throw result.error;return(result.data??[]) as ReportRow[]}
async function dashboardStatus(){const session=await latestSession(),[nativeEngine,profitEngine]=await Promise.all([latestEngineTick("NATIVE"),latestEngineTick("PROFIT")]),now=Date.now(),engineLastAt=[nativeEngine?.observed_at,profitEngine?.observed_at].filter(Boolean).sort().at(-1) as string|undefined;const base={schema_version:"brian.control-center.status.v1",generated_at:new Date().toISOString(),source_experiment_id:SOURCE_EXPERIMENT_ID,engine:{online:Boolean(engineLastAt)&&now-Date.parse(engineLastAt!)<12*60_000,last_tick_at:engineLastAt??null,tick_age_seconds:engineLastAt?Math.max(0,Math.round((now-Date.parse(engineLastAt))/1000)):null,cadence_seconds:300},telegram:{configured:Boolean((Deno.env.get("TELEGRAM_BOT_TOKEN")??"").trim()&&(Deno.env.get("TELEGRAM_CHAT_ID")??"").trim())},shadow_only:true,live_execution:false};if(!session)return{...base,session:null,policies:null,reports:[]};const startingEquity=Number(session.start.starting_equity??500),endAt=session.ended_at,[nativeTicks,profitTicks,reports]=await Promise.all([loadTicks(session.start.source_experiment_id,"NATIVE",session.start.requested_at,endAt),loadTicks(session.start.source_experiment_id,"PROFIT",session.start.requested_at,endAt),loadReports(session.start.session_id)]);return{...base,session:{session_id:session.start.session_id,status:session.active?"RUNNING":"PAUSED",started_at:session.start.requested_at,ended_at:session.ended_at,starting_equity:startingEquity,policy_scope:session.start.policy_scope,source_experiment_id:session.start.source_experiment_id},policies:{NATIVE:{...derivePolicySnapshot(nativeTicks,startingEquity),decisions:topDecisionDiagnostics(nativeTicks)},PROFIT:{...derivePolicySnapshot(profitTicks,startingEquity),decisions:topDecisionDiagnostics(profitTicks)}},reports}}
async function startSession(body:Record<string,unknown>){const startingEquity=Number(body.starting_equity??1000),policyScope=String(body.policy_scope??"BOTH").toUpperCase();if(!Number.isFinite(startingEquity)||startingEquity<=0||startingEquity>1_000_000)throw new Error("STARTING_EQUITY_OUT_OF_RANGE");if(!["BOTH","NATIVE","PROFIT"].includes(policyScope))throw new Error("INVALID_POLICY_SCOPE");const sessionId=`mcp-${new Date().toISOString().replace(/[-:.TZ]/g,"").slice(0,14)}-${crypto.randomUUID().slice(0,8)}`,eventId=`evt-${crypto.randomUUID()}`,result=await supabase.rpc("brian_dashboard_start_session",{p_event_id:eventId,p_session_id:sessionId,p_starting_equity:startingEquity,p_policy_scope:policyScope,p_source_experiment_id:SOURCE_EXPERIMENT_ID});if(result.error)throw result.error;return{status:"STARTED",session_id:sessionId,starting_equity:startingEquity,policy_scope:policyScope,shadow_only:true,live_execution:false}}
async function pauseSession(){const session=await latestSession();if(!session||!session.active)return{status:"ALREADY_PAUSED",shadow_only:true,live_execution:false};const result=await supabase.rpc("brian_dashboard_pause_session",{p_event_id:`evt-${crypto.randomUUID()}`,p_session_id:session.start.session_id});if(result.error)throw result.error;return{status:"PAUSED",session_id:session.start.session_id,shadow_only:true,live_execution:false}}
async function restartSession(body:Record<string,unknown>){const startingEquity=Number(body.starting_equity??1000),policyScope=String(body.policy_scope??"BOTH").toUpperCase();if(!Number.isFinite(startingEquity)||startingEquity<=0||startingEquity>1_000_000)throw new Error("STARTING_EQUITY_OUT_OF_RANGE");if(!["BOTH","NATIVE","PROFIT"].includes(policyScope))throw new Error("INVALID_POLICY_SCOPE");const sessionId=`mcp-${new Date().toISOString().replace(/[-:.TZ]/g,"").slice(0,14)}-${crypto.randomUUID().slice(0,8)}`,result=await supabase.rpc("brian_dashboard_restart_session",{p_pause_event_id:`evt-${crypto.randomUUID()}`,p_start_event_id:`evt-${crypto.randomUUID()}`,p_new_session_id:sessionId,p_starting_equity:startingEquity,p_policy_scope:policyScope,p_source_experiment_id:SOURCE_EXPERIMENT_ID});if(result.error)throw result.error;return{status:"RESTARTED",session_id:sessionId,starting_equity:startingEquity,policy_scope:policyScope,shadow_only:true,live_execution:false}}
async function telegramSend(text:string):Promise<{attempted:boolean;sent:boolean;error:string|null}>{const token=(Deno.env.get("TELEGRAM_BOT_TOKEN")??"").trim(),chatId=(Deno.env.get("TELEGRAM_CHAT_ID")??"").trim();if(!token||!chatId)return{attempted:false,sent:false,error:"telegram secrets not configured"};try{const response=await fetch(`https://api.telegram.org/bot${token}/sendMessage`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({chat_id:chatId,text,disable_web_page_preview:true}),signal:AbortSignal.timeout(8000)});if(!response.ok)return{attempted:true,sent:false,error:`Telegram HTTP ${response.status}`};return{attempted:true,sent:true,error:null}}catch(error){return{attempted:true,sent:false,error:String(error instanceof Error?error.message:error)}}}
async function persistReport(session:SessionState,policy:"NATIVE"|"PROFIT",windowStart:string,windowEnd:string,allTicks:ShadowTick[]):Promise<{inserted:boolean;report:HourlyReportPayload;telegram:{attempted:boolean;sent:boolean;error:string|null}}>{const startingEquity=Number(session.start.starting_equity??500),report=buildHourlyReport(session.start.session_id,policy,allTicks,startingEquity,windowStart,windowEnd),reportId=await sha256Hex(`${session.start.session_id}|${policy}|${windowStart}|${windowEnd}`),existing=await supabase.from("brian_dashboard_hourly_reports").select("report_id,payload,telegram_attempted,telegram_sent,telegram_error").eq("report_id",reportId).maybeSingle();if(existing.error)throw existing.error;if(existing.data)return{inserted:false,report:existing.data.payload as HourlyReportPayload,telegram:{attempted:Boolean(existing.data.telegram_attempted),sent:Boolean(existing.data.telegram_sent),error:existing.data.telegram_error?String(existing.data.telegram_error):null}};const telegram=await telegramSend(formatTelegramReport(report)),insert=await supabase.from("brian_dashboard_hourly_reports").insert({report_id:reportId,session_id:session.start.session_id,policy_kind:policy,window_start:windowStart,window_end:windowEnd,payload:report,telegram_attempted:telegram.attempted,telegram_sent:telegram.sent,telegram_error:telegram.error,evidence_class:EVIDENCE_CLASS,shadow_only:true,live_execution:false});if(insert.error)throw insert.error;return{inserted:true,report,telegram}}
async function generateDueHourlyReports(forceNow=false){const session=await latestSession();if(!session)return{status:"NO_SESSION",reports:[]};const now=new Date(),sessionStart=new Date(session.start.requested_at),effectiveEnd=session.ended_at?new Date(session.ended_at):now,existingReports=await loadReports(session.start.session_id),outcomes=[];for(const policy of ["NATIVE","PROFIT"] as const){if(session.start.policy_scope!=="BOTH"&&session.start.policy_scope!==policy)continue;const policyReports=existingReports.filter(report=>report.policy_kind===policy),lastEndMs=policyReports.length?Math.max(...policyReports.map(report=>Date.parse(report.window_end))):sessionStart.getTime(),elapsed=effectiveEnd.getTime()-lastEndMs;if(!forceNow&&elapsed<60*60_000)continue;const windowStart=new Date(lastEndMs),windowEnd=forceNow?effectiveEnd:new Date(Math.min(lastEndMs+60*60_000,effectiveEnd.getTime()));if(windowEnd.getTime()<=windowStart.getTime())continue;const ticks=await loadTicks(session.start.source_experiment_id,policy,session.start.requested_at,windowEnd.toISOString());outcomes.push({policy,...(await persistReport(session,policy,windowStart.toISOString(),windowEnd.toISOString(),ticks))})}return{status:outcomes.length?"GENERATED":"NOT_DUE",reports:outcomes}}

Deno.serve(async(req:Request)=>{const origin=req.headers.get("origin");if(req.method==="OPTIONS")return new Response("ok",{headers:corsHeaders(origin)});if(req.method!=="POST")return jsonResponse({status:"METHOD_NOT_ALLOWED"},405,origin);try{const body=await req.json().catch(()=>({})) as Record<string,unknown>,action=String(body.action??"status");if(action==="hourly_report"){await requireCronAuth(req);return jsonResponse(await generateDueHourlyReports(false),200,origin)}await requireDashboardAuth(req);if(action==="status")return jsonResponse(await dashboardStatus(),200,origin);if(action==="start")return jsonResponse(await startSession(body),200,origin);if(action==="pause")return jsonResponse(await pauseSession(),200,origin);if(action==="restart")return jsonResponse(await restartSession(body),200,origin);if(action==="report_now")return jsonResponse(await generateDueHourlyReports(true),200,origin);return jsonResponse({status:"UNKNOWN_ACTION",action},400,origin)}catch(error){const message=String(error instanceof Error?error.message:error),unauthorized=message.includes("UNAUTHORIZED_");console.error("brian-control-center failed",{message,unauthorized});return jsonResponse({status:unauthorized?"UNAUTHORIZED":"FAILED_CLOSED",error:message,shadow_only:true,live_execution:false},unauthorized?401:500,origin)}});
